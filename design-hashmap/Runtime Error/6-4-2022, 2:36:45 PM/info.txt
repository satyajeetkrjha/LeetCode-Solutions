{"id":714380811,"lang":"cpp","time":"2 months, 3 weeks","timestamp":1654367805,"status_display":"Runtime Error","runtime":"N/A","url":"/submissions/detail/714380811/","is_pending":"Not Pending","title":"Design HashMap","memory":"N/A","code":"class MyHashMap {\npublic:\n    \n    vector < vector <pair <int ,int>>> vec;\n    int sz ;\n    MyHashMap() {\n        sz =1000;\n        vec.resize(1000);\n    }\n    \n    void put(int key, int value) {\n        \n        int bucket = key % sz;\n        vector <pair <int ,int> > &temp = vec[bucket];\n        \n        for(auto it = temp.begin();it!=temp.end();it++){\n            if(it->first == key){\n                it->second = value ;\n                return ;\n            }\n        }\n        temp.push_back({key ,value});\n        \n        \n        \n    }\n    \n    int get(int key) {\n        int bucket = key %sz;\n        vector <pair <int ,int> > &temp = vec[bucket];\n        for(auto it = temp.begin();it!=temp.end();it++){\n            if(it->first == key){\n                return it->second;\n            }\n        }\n        return -1;\n    }\n    \n    void remove(int key) {\n        int bucket = key %sz;\n        vector <pair <int ,int> > &temp = vec[key];\n        for(auto it =temp.begin();it!=temp.end();it++){\n            if(it->first == key){\n             temp.erase(it);\n                return ;\n            }\n        }\n        \n    }\n};\n\n/**\n * Your MyHashMap object will be instantiated and called as such:\n * MyHashMap* obj = new MyHashMap();\n * obj->put(key,value);\n * int param_2 = obj->get(key);\n * obj->remove(key);\n */","compare_result":"111111111111111111111110000000000000","title_slug":"design-hashmap"}