{"id":777813100,"lang":"cpp","time":"6 days, 10 hours","timestamp":1660920818,"status_display":"Accepted","runtime":"37 ms","url":"/submissions/detail/777813100/","is_pending":"Not Pending","title":"Evaluate Reverse Polish Notation","memory":"11.9 MB","code":"class Solution {\npublic:\n    int evalRPN(vector<string>& tokens) {\n      stack <int> st;\n      for(int i =0 ;i<tokens.size();i++){\n          string ch = tokens[i];\n          if(ch == \"+\" || ch == \"*\" || ch == \"/\" || ch == \"-\"){\n              auto first = st.top();st.pop();\n              auto second = st.top();st.pop();\n              if(ch == \"+\") st.push(first+second);\n              if(ch == \"-\") st.push(second-first);\n              if(ch == \"/\") st.push(second/first);\n              if(ch == \"*\") st.push(first*second);\n           }\n          else\n              st.push(stoi(ch));\n      }\n        return st.top();\n    }\n};","compare_result":"11111111111111111111","title_slug":"evaluate-reverse-polish-notation"}